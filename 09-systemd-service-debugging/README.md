# Systemd Service Debugging

**Issue:** A systemd service failed to start or was in a failed state.

**Checks:** Verified service status, inspected logs, checked configuration files, and analyzed dependencies.

**Result:** Identified the root cause of failure and restored the service.

**Key Learning:** systemctl status + journalctl are the first tools for any service failure.
