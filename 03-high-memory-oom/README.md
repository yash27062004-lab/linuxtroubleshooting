# High Memory / OOM Troubleshooting

**Issue:** System memory usage became critically high, causing processes to slow down or get terminated.

**Checks:** Verified memory usage, swap utilization, and reviewed OOM killer logs.

**Result:** Identified memory-consuming processes and confirmed the root cause.

**Key Learning:** High memory usage is not always a problem; memory pressure and OOM events are what matter.
